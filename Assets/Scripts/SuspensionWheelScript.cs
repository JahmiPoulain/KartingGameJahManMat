using UnityEngine;

public class Suspention : MonoBehaviour
{
    /*Vector3 startPos;
    private void Start()
    {
        startPos = transform.localPosition;
    }
    private void FixedUpdate()
    {
        RaycastHit hit;
        if (Physics.Raycast(transform.position, Vector3.down, out hit, transform.localScale.x / 2f + 0.05f))
        {
            transform.position = new Vector3 (transform.position.x, hit.point.y + transform.localScale.x / 2f + 0.05f, transform.position.z);
            //groundNormal = hit.normal;
            //float nextYPos = transform.position.y + 0.5f * Time.fixedDeltaTime;
            //if (nextYPos < hit.point.y)
            //transform.localPosition += new Vector3(transform.position.x, hit.point.y + 0.54f, transform.position.z);
        }
        else //if (transform.localPosition.y > startPos.y)
        {
            //transform.localPosition -= new Vector3(0, 1f * Time.fixedDeltaTime, 0);
            transform.localPosition = startPos;
        }
    }*/
}
